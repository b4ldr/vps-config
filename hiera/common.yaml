base::packages:
  - vim
  - git
  - ruby-bundler
  - facter
  - puppet
  - zsh
  - dnsutils
base::users:
  jbond:
    user:
      shell: /bin/zsh
      groups:
        - sudo
      managehome: true
      membership: minimum
    ssh_authorized_key:
      ensure: present
      type: ssh-ed25519
      key: 'AAAAC3NzaC1lZDI1NTE5AAAAIKWgKgk0wUrZkapYfUq57G5o7gkVWM+FzsHOEnT9hQpA'
my_fw::pre::rules:
  '000 accept all icmp':
    proto: 'icmp'
    action: 'accept'
  '001 accept all to lo interface':
    proto: 'all'
    iniface: 'lo'
    action : 'accept'
  '002 reject local traffic not on loopback interface':
    iniface: '! lo'
    proto: 'all'
    destination: '127.0.0.1/8'
    action: 'reject'
  '003 accept ssh':
    proto: 'tcp'
    dport: 22
    action: 'accept'
  '004 accept related established rules':
    proto: 'all'
    state: ['RELATED', 'ESTABLISHED']
    action: 'accept'
  '000 accept all icmp ipv6':
    proto: 'icmp'
    action: 'accept'
    provider: ip6tables
  '001 accept all to lo interface ipv6':
    proto: 'all'
    iniface: 'lo'
    action : 'accept'
    provider: ip6tables
  '002 reject local traffic not on loopback interface ipv6':
    iniface: '! lo'
    proto: 'all'
    destination: '::1'
    action: 'reject'
    provider: ip6tables
  '003 accept ssh ipv6':
    proto: 'tcp'
    dport: 22
    action: 'accept'
    provider: ip6tables
  '004 accept related established rules ipv6':
    proto: 'all'
    state: ['RELATED', 'ESTABLISHED']
    action: 'accept'
    provider: ip6tables
